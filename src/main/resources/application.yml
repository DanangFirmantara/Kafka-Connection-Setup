server:
    port: 8081

spring:
    kafka:
        producer:
            bootstrap-servers : localhost:9092
        # consumer:
        #     bootstrap-servers : localhost:9092
        #     group-id: group-1